package uk.ac.manchester.dstoolkit.service.impl.morphisms.matching;

/***
 * 
 * The result of a matcher usually shows WHICH elements are matched and HOW the elements are match (mapping expression). The mapping
 * expressions may be directional or un-directional. Below we only consider equality relations therefore we consider their directions
 * as BOTH.
 *
 */

public enum DirectionType {
	BOTH;
}
