package uk.ac.manchester.dstoolkit.domain.models.canonical;

/**
 * @author chedeler
 *
 */
public enum DataType {
	BYTE, INTEGER, BIGINTEGER, LONG, SHORT, BIGDECIMAL, DOUBLE, FLOAT, BOOLEAN, DATE, TIME, TIMESTAMP, CHAR, STRING, TEXT, GEOCOORD, SERIAL, SET, ENUM, BLOB, CLOB, SYSNAME, IMAGE, ROWID, SDO_GEOMETRY, XMLTYPE;
}
